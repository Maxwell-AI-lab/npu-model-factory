# NPU Model Factory · 方案设计总纲

> 版本：v1.0（2026-09-03）
> 定位：把"新模型在昇腾 NPU 上跑通后训练 RL"从人工踩坑变成角色清晰、
> 门禁严格、全程可溯源的流水线。
> 本仓为方案与模板层；具体模型的补丁与镜像在交付仓按本方案组织。

---

## 1. 四角色体系

```
① 模型适配工程师 ──补丁归档──┐
                             ├──→ CIE 工程师 ──正式版──→ ② 训练工程师
③ 特性开发工程师 ──PR 合入───┘      （版本集成·质量验证）      （直接跑训练）
        ↑ 基础版（第一级发布） ↑              │
        └──────────────────────┘         （②故障回流回 CIE）
```

| 角色 | 一句话职责 | 输入 | 输出 |
|---|---|---|---|
| ① 模型适配工程师 | 新模型打通推理/训练/RL | 新模型 + 验证数据集 | 补丁 + 基础镜像 |
| ③ 特性开发工程师 | 新特性集成验证 | ①基础版 + 分支基线 | 特性补丁 + 特性镜像 |
| CIE 工程师 | 合并管理·质量门禁·镜像制作·正式发布 | 补丁（PR）+ 故障回流 | 正式版交付包 |
| ② 训练工程师 | 直接使用镜像跑训练 | 发布版 + 权重 + 数据 | 训练产物 + 故障回流 |

完整定义见 [roles/ROLES.md](roles/ROLES.md)。可视化：[index.html](index.html)（全景图）。

## 2. CIE 串行管线（不可跳步）

```
Git 分支 ──────→ 质量门禁 + 镜像制作 ──────→ 正式版本发布
合并管理·G1       G2-G4 验收·Dockerfile      定期班车·Stable·三元组锁定
```

1. **代码合并管理**：接收①补丁归档 / ③PR / ②故障回流；G1 门禁（patch
   可应用性 + import 自检 + PR 附自验输出）；管理 overlay patch 单一事实源
2. **质量门禁**：G2 双镜像对比（与上一版逐项对账）→ G3 冒烟 + 制品登记
   → G4 运行验收（≥5 步指标对照基线）
3. **镜像制作**：Dockerfile 可复现构建；tag 递增永不覆盖已验证基线
4. **正式版本发布**：定期班车 + Stable 晋级 + 三元组锁定（镜像+权重+数据
   sha256）+ 交付包（镜像 + 使用 README + 使用侧 skill）

核心原则：**质量责任与开发责任分离**——①③只管做对补丁；能不能合并、
出不出镜像、何时发布，由 CIE 独立判断。

## 3. 两级发布节奏

```
第一级 · 基础版（事件驱动）：① 验收完成 → CIE 即时发布 → ③ 的开发基座
第二级 · 正式版（定期班车）：③ 多特性合入验收 → CIE 定期发布 → ② 使用
```

版本号：基础版 `vN`；正式版 `vN.x-<特性集>`；紧急修复插班（补丁号）。
升级基线 = 下一班基础版。

## 4. 资产与可溯源

| 资产 | 管理处 | 溯源方式 |
|---|---|---|
| 组件代码改动 | Git 分支 overlay/（patch + new-files） | patch 整树重生成（`git diff HEAD`），commit 历史即特性边界 |
| 镜像 | registry/manifest.yaml | 每 tag 登记：组件版本、镜像 sha256、**三元组**（权重/数据 sha256）、基线指标、验收结论 |
| 数据集 | registry/datasets.yaml | 来源 + 生成脚本 + 校验和 |
| 经验 | knowledge/ | 模型无关的坑提升至 npu-gotchas，模型专属留在 reports |

铁律（每条有真实事故背书）：
1. patch 整树重生成，不手工拼增量
2. `git diff HEAD`（含 staged），不是 `git diff`
3. 新文件走 new-files/，不进 diff
4. 镜像 tag 递增不覆盖
5. 合入 ≠ 完成：G1-G4 全过才算

## 5. 质量门禁 G1-G4（详见 docs/quality-gates.md）

| 门禁 | 时机 | 内容 | 工具 |
|---|---|---|---|
| G1 代码 | PR 合入前 | patch 可应用 + import 自检 + 自验输出 | cie/g1_code_gate.sh |
| G2 构建 | build 后 | 双镜像对比（git 状态/md5/import 三对账） | cie/g2_build_compare.sh |
| G3 镜像 | 发布登记前 | 冒烟 + manifest 登记 | cie/g3_image_register.sh |
| G4 运行 | 发布前最后 | ≥5 步 pearson/score/TPOT/乱码/崩溃对照基线 | cie/g4_run_acceptance.sh |

## 6. 分场景验收（详见 docs/acceptance-matrix.md）

验收深度按变更类型定：**S1 新模型**（全量+立基线）/ **S2 新特性**
（生效实证+不回退+可回退）/ **S3 升级**（全量回归+对比+历史坑复查）/
**S4 修复**（定点+回归）/ **S5 重构**（等价性证明）。

## 7. 目录结构

```
├── DESIGN.md            # 本文件
├── index.html           # 全景图（交互）
├── roles/ROLES.md       # 四角色完整定义
├── cie/                 # CIE 管线脚本（G1-G4 + release + check_health）
├── registry/            # manifest（三元组台账）+ datasets 登记
├── templates/           # ①工作区 / ③流程 / Dockerfile 模板
├── docs/                # 门禁详规 + 验收场景矩阵
└── knowledge/           # 跨模型通用坑（种子来自实战）
```

## 8. 与历史经验的关系

方法论继承自 model-factory 的 6-skill 流水线（9 宫格、证据链、
训推一致第一原则）；本方案的三点升级：
1. **角色中心化**：从"隐含单一执行者"到四角色显式分工，CIE 独立把关
2. **两级发布**：基础版/正式版分离，③有了明确的开发基座
3. **三元组溯源**：镜像可信从"代码可复现"扩展到"运行输入可锁定"
